<resources>
    <!-- CaseDesk AI dark theme — WebView background se match karta hai, white flash nahi -->
    <style name="Theme.CaseDeskAI" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/brand_dark</item>
        <item name="android:statusBarColor">@color/brand_dark</item>
        <item name="android:navigationBarColor">@color/brand_dark</item>
        <item name="colorPrimary">@color/brand_indigo</item>
        <item name="colorPrimaryDark">@color/brand_indigo_dark</item>
    </style>
</resources>
